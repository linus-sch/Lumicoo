<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@24,400,0,0" />
  <title>Contact Form</title>
  <style>
    body {
      font-family: 'Helvetica Neue', sans-serif;
      background-color: #f9f9f9;
    }

    .container {
      max-width: 450px;
      margin: 40px auto;
      padding: 20px;
      background-color: #ffffff;
      border: 2px solid #000;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
    }

    form {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 5px;
    }

    input[type="text"],
    input[type="email"],
    textarea {
      width: 100%;
      margin-bottom: 15px;
      padding: 10px;
      border: 2px solid #000;
      border-radius: 5px;
      font-size: 16px;
      font-family: 'Helvetica Neue', sans-serif; /* Schriftart explizit setzen */
    }

    input[type="submit"] {
      background-color: #3F50B5;
      color: white;
      cursor: pointer;
      padding: 10px 20px;
      border: 2px solid #000;
      border-radius: 5px;
      font-size: 16px;
      transition: background-color 0.3s;
      width: 100%;
      margin-bottom: 5px;
    }

    input[type="submit"]:hover {
      background-color: #303134;
    }
  </style>
</head>
<body>
<p style="text-align: left;">


<div class="container">
<p style="text-align: left;">
  <a href="https://www.lumicoo.net" style="text-decoration: none; color: #000;">
    <span class="material-symbols-rounded" style="vertical-align: middle;">arrow_back</span>
  </a>
</p>
  <form id="contact-form" name="contact-form">
    <h1>Contact</h1>
    <input type="text" name="name" placeholder="Name" required>
    <input type="email" name="email" placeholder="Email" required>
    <textarea name="message" rows="7" placeholder="Message" required></textarea>
    <input type="hidden" name="source" value="Lumicoo.net">
    <input type="submit" value="Submit" id="submit">
  </form>
  <div id="feedback"></div>
</div>
<p>
<p style="text-align: center;">
  <a href="mailto:contact-net@lumicoo.com" style="text-decoration: none; color: #000;">Email</a> | <a href="https://www.lumicoo.net/tnlt/datenschutz" style="text-decoration: none; color: #000;">Datenschutz</a>
</p>

<script>
  const scriptURL = 'https://script.google.com/macros/s/AKfycbyMxg4QHdtFOlH13KY45tAujfiaNktLy21XCAL4PXOykY9C1GDGCL8hZQUJxEN4nAwC/exec';
  const form = document.forms['contact-form'];
  const feedback = document.getElementById('feedback');

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    const submitBtn = document.getElementById('submit');
    submitBtn.value = 'Sending...';

    const formData = new FormData(form);

    try {
      const response = await fetch(scriptURL, {
        method: 'POST',
        body: formData
      });

      if (response.ok) {
        form.reset();
      } else {
        feedback.innerText = 'Error! There was a problem submitting your form.';
      }
    } catch (error) {
      feedback.innerText = 'Error!';
      console.error('Error!', error.message);
    } finally {
      submitBtn.value = 'Submit';
    }
  });
</script>

</body>
</html>
